"""
Experiment tracking and hyperparameter optimisation.

Optional integrations — gracefully disabled when dependencies not installed::

    from privateye.tracking.mlflow_tracker import MLflowTracker
    from privateye.tracking.optuna_optimizer import OptunaOptimizer

Install optional deps::

    pip install privateye[phase0]
    # or individually:
    pip install mlflow optuna
"""
